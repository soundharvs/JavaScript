var isLoggedIn = true;
var isEmailVerified = false;
var creditCardDetails = true;

if (isLoggedIn && isEmailVerified && creditCardDetails){
    console.log("Allow the user to make a Purchase");
} else{
    console.log("Don't allow to purchase");
}